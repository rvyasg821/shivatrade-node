export enum ENUM_REBATE_STATUS {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

/** Whether `pct` is interpreted as a percentage of the line, or a flat amount. */
export enum ENUM_REBATE_TYPE {
    PERCENT = 'percent',
    FIXED = 'fixed',
}
