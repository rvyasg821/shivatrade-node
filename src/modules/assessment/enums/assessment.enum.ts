export enum ENUM_ASSESSMENT_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum ENUM_ASSESSMENT_REPORT_STATUS {
    PENDING = 1,
    COMPLETED = 2,
    ARCHIVED = 3,
}

export enum ENUM_SECTION_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum ENUM_QUESTION_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum ENUM_QUESTION_ANSWER_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum ENUM_QUESTION_OPTION_TYPE {
    SINGLE_CHOICE = 'single_choice',
    MULTIPLE_CHOICE = 'multiple_choice',
    TEXT = 'text',
    NUMBER = 'number',
}