/** Mirrors the other masters so the whole app shares one status vocabulary. */
export enum ENUM_UOM_STATUS {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
}

export const UOM_STATUS_VALUES = Object.values(ENUM_UOM_STATUS);
