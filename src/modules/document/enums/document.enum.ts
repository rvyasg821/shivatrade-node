export enum ENUM_DOCUMENT_STATUS {
    ACTIVE = 'active',
    ARCHIVED = 'archived',
    EXPIRED = 'expired',
    PENDING_REVIEW = 'pending_review',
}

export enum ENUM_DOCUMENT_APPROVAL_STATUS {
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected',
}
