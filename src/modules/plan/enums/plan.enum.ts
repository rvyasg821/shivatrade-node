export enum ENUM_PLAN_STATUS {
    ACTIVE = 1,
    INACTIVE = 0,
}

export enum ENUM_PLAN_BILLING_CYCLE {
    MONTHLY = 'MONTHLY',
    YEARLY = 'YEARLY',
}

export enum ENUM_PLAN_CURRENCY {
    USD = 'USD',
    EUR = 'EUR',
    GBP = 'GBP',
    INR = 'INR',
}

export enum ENUM_PLAN_DURATION_TYPE {
    MONTHLY = 'MONTHLY',
    WEEKLY = 'WEEKLY',
    YEARLY = 'YEARLY',
    TRIAL = 'TRIAL',
    LIFETIME = 'LIFETIME',
}

export enum ENUM_TOOL_PRICING_MODE {
    FIXED = 'fixed',
    MULTIPLIER = 'multiplier',
}